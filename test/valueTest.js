const deviceFaultCode = require('./../dist/deviceFaultCode').default,
     deviceAlarmCode = require('./../dist/deviceAlarmCode').default;

console.log(deviceAlarmCode);
console.log(deviceFaultCode);
console.log(deviceFaultCode.videoLost);