'use strict';

let schoolLogType = {
    login: {
        name: '登录',
        value: 0
    },
    operation: {
        name: '操作',
        value: 1
    }
};

Object.freeze(schoolLogType);
export default schoolLogType;
